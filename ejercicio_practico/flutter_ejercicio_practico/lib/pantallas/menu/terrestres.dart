import 'package:flutter/material.dart';

class Terrestres extends StatelessWidget {
  const Terrestres({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child:Text(
      'Hola',
      style: TextStyle(fontSize: 30), //estilo de fuente y tamaño,color
    ),
    );
  }
}